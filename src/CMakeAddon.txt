
if (NOT WIN32)
  target_link_libraries(${PROJECT_NAME}
    PUBLIC
      -lglfw
      -ldl
      -pthread
  )
  
  target_compile_options(${PROJECT_NAME}
    PUBLIC
      -pthread
  )
endif()

target_link_libraries(${PROJECT_NAME}
  PRIVATE
    shaders
)
